interface:
  display_name: "Context Modeler"
  short_description: "Repo-specific patterns and workflows for context-modeler"
  default_prompt: "Use the context-modeler repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true